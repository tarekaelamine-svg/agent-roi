apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-roi.name" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "agent-roi.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
